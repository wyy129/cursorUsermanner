{
  "name": "cursor-user-manager",
  "version": "1.2.0",
  "description": "Cursor用户数据管理系统 - 支持数据导入、Token查询和后端代理",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "proxy": "cd server && npm start",
    "setup": "npm install && cd server && npm install && cd .."
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}

